This is the temporary file for this folder to commit and the folder holds all the resources that are static i.e., doesn't interact with the browser directly e.g. css, js, images and other static resources.